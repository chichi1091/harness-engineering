# Harness Engineering 運用指示

このリポジトリでは、AI ランタイムに関係なく、`agents/`、`commands/`、`workflows/` を正本として扱う。

## 基本原則

- 利用者から受け取る主入力は「何を作りたいか」である。
- 役割はモデル名ではなく能力と責務で定義する。ランタイムやモデルを定義ファイルに埋め込まない。
- Agent間では会話履歴やコード全文ではなく、構造化Artifactを基本の受け渡し単位とする。次のAgentは前のAgentの会話履歴を引き継がず、Workflowに指定されたArtifactを受け取り、Artifactだけでは判断できない情報のみ追加取得する。
- 実装前に Architect、次に読み取り専任の Explorer を実行する。
- Explorer はリポジトリを変更してはならない。
- Developer は承認済みの設計と Explorer の報告を入力として変更する。
- Test Engineer、Reviewer、Documentation の完了基準を満たして初めてワークフローを完了とする。
- 不確実な点、設計上の選択、影響範囲は成果物に明記する。推測を確定事項として扱わない。

## 最小実行プロトコル

1. 利用者の依頼に合う `commands/` を選ぶ。
2. コマンドが参照する `workflows/` を上から順に実行する。
3. 各ステップでは該当する `agents/` 定義の責務、制約、完了条件に従う。
4. ステップの出力を次ステップへそのまま渡せる短い成果物として残す。
5. 中断・差し戻し時は、直前の成果物と理由を残して適切なステップへ戻る。

## ランタイム互換性

このファイルと YAML/Markdown 定義は、Codex、OpenCode、Claude Code、Gemini CLI 等で解釈するための共通層である。
ランタイム固有の設定・自動変換・検証は、将来の Adapter レイヤーで追加する。現段階では、各ランタイムの実行者がこの共通仕様を読み、手動でステップを進める。
