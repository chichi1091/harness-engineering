{
  "name": "harness-engineering",
  "private": true,
  "version": "0.1.0",
  "description": "Runtime-independent AI development orchestration framework",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "validate:yaml": "node scripts/validate-yaml.js",
    "validate:workflows": "node scripts/validate-workflows.js",
    "validate:profiles": "node scripts/validate-profiles.js"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
